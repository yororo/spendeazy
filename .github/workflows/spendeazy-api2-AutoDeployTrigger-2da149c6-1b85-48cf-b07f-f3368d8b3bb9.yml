name: Trigger auto deployment for spendeazy-api2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/spendeazy-api2-AutoDeployTrigger-2da149c6-1b85-48cf-b07f-f3368d8b3bb9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.SPENDEAZYAPI2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SPENDEAZYAPI2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SPENDEAZYAPI2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/api
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: docker.io
          registryUsername: ${{ secrets.SPENDEAZYAPI2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SPENDEAZYAPI2_REGISTRY_PASSWORD }}
          containerAppName: spendeazy-api2
          resourceGroup: spendeazy-dev
          imageToBuild: syoro/spendeazy-api2:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


